{
  "framework": "vite",
  "installCommand": "npm --prefix Styra install",
  "buildCommand": "npm --prefix Styra run build",
  "outputDirectory": "Styra/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
